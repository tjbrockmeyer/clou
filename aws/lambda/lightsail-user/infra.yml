Version: master
Template: custom-resource
AppName: lightsail-user
Regions:
  - us-east-1
BuildOverride: rm -rf .aws-sam && npm run prepack

Parameters:
  Name: lightsail-user
  Runtime: nodejs14.x
  Handler: dist/index.js
  
Substitution:
  PolicyStatements:
    - Effect: Allow
      Action:
        - lightsail:GetInstance
      Resource: 
        Fn::Sub: arn:aws:lightsail:*:${AWS::AccountId}:*
